# Roadmap — Estabilización CI TanStack Start

- [x] Fix build CI: nitro output explícito a dist/ (v8.0.3) — confirmado en GitHub run 34190853585
- [ ] Limpieza knip exports/types (informativo, `bun run knip:deep`): knip ya no corre en CI; pendiente de bajo valor
- [x] A) E2E falla: `__name is not defined` transversal (desktop+mobile, smoke-nav.spec.ts:70/99 y otras suites). Investigar causa real (transforms, page.evaluate/addInitScript serializadas, bundles). NO parche con global genérico. Shard1: portal-statement sin auth token en localStorage tras 30s + expect visible. Verificar login/storage con migración.
- [x] B) SQL smoke `supabase/tests/r_fix40_contratos_duplicado_trigger_smoke.sql` falla (T1 y T6): depende de datos productivos (CTR-0002/CTR-0003, admin user_roles). Reescribir autocontenido con fixtures en transacción + rollback, preservando casos (duplicado histórico, concurrencia/candado, cancelación, reactivación). Validar en Supabase local. No usar continue-on-error para declarar verde.
- [x] SEGURIDAD (prioridad 1): guard fail-closed anti-producción en E2E (productionGuard.ts + setup/teardown/seed/supabaseEnv + precondición en CI). Pendiente de verificación real: provisionar backend Supabase aislado para el job e2e (secrets E2E_SUPABASE_*).
- [x] C) Auditoría lean de workflows (v8.2.0): ci.yml con 2 jobs base + condicionales (sin ci-success, sin knip), smoke de arranque CON interacción sobre el build real y red externa bloqueada, tests Deno offline en CI, smoke SQL bloqueante (42/42), prod-smoke horario, permisos al mínimo; guard de producción con resolución única compartida con apiAuth. Retirados bundle-size/lighthouse/changelog-check/e2e-on-demand/extract-rls-junit/check-selfcontained-smoke. Ver docs/ci.md.
- [ ] Verificación final en GitHub (requiere push): build con config GitHub + bun frozen lockfile, E2E contra ese artefacto, suites SQL. Reportar SHA, causas, comandos/resultados, pendientes. No declarar CI verde hasta confirmación de GitHub.

- [ ] BLOQUEADO (requiere al usuario): provisionar backend Supabase aislado para correr las E2E completas A MANO. Ya no existe workflow de E2E: mientras la base de la app sea la productiva, no debe haber botón que las dispare.

Restricciones: YAGNI, no publicar, no tocar datos productivos/secretos/protecciones, no bajar cobertura ni quitar tests/reglas, no silenciamientos, preservar cambios ajenos en HEAD (0b7b242c).

- [x] Reparación desfase migraciones (Lote A y Lote B): objetos faltantes, guards financieros, daños, ciclo de vida y validate_transition aplicados en producción sin escrituras de datos.
- [ ] PENDIENTE (requiere autorización del usuario): reconciliaciones históricas (16 facturas 'paid' con saldo, estados de montacargas), recálculo de hashes bancarios, cambios de permisos de inserción en tablas bancarias y (des)programación de cron.

- [x] Verificación de la cadena 0024→0025→0026 en base efímera (CI): suite RLS que prueba orden secuencial, policies org-scoped que reemplazan las globales, fail-closed de organización, portal con rol operativo residual, 42501 en cruces de folio REP, wrapper de 2 args sin authenticated, reconciliación service_role idempotente, y guard que demuestra la brecha 42883 si falta 0025. Cerrada en commit `0a941a4a` (8.10.5/8.10.6): RLS 54/54 run 35234484524, smoke 45/45, CI principal run 35234484453, Gitleaks run 35234484442. Migración 0026 sigue pendiente de rollout productivo (journal en 0023); no se ejecutó en producción.
- [x] Cierre Storage multiempresa: commit 45c9293 confirmó RLS 54/54 y smoke 45/45 en GitHub Actions (runs 35180736054/35180736071/35180736167). Rutas nuevas con prefijo aisladas; legado de documentos aislado por ficha dueña; legado de los demás buckets sigue compartido. Alta de segunda empresa bloqueada hasta migrar los objetos históricos.
- [x] Migrador de Storage: separar copia/verificación del borrado (8.13.0). `apply` ya sólo inventaría, copia, verifica destino y actualiza referencias; deja todas las fuentes intactas y nunca marca `source_deleted`. Fase nueva `delete_sources` con bandera propia `STORAGE_MIGRATION_DELETE_SOURCES_ENABLED` (apagada), confirmación `DELETE_MIGRATED_SOURCES_AFTER_VERIFY`, inventario completo, referencias ya apuntando al destino y destino verificado; huérfanos nunca elegibles (`apply_orphans` sólo copia). Pruebas en `supabase/functions/_shared/storageDeletePhase_test.ts`.
- [x] Migrador de Storage: verificación de integridad real (8.13.1). `_shared/storageCopyVerification.ts` compara tamaño y SHA-256 de los bytes de fuente y copia; se aplica en `apply` tras copiar y también cuando el destino ya existía, y se revalida en `delete_sources` inmediatamente antes de remover (ningún estado previo del ledger la evita). Discrepancia ⇒ `failed`/`blocked` con código agregado y sin tocar referencias ni borrar. Pruebas en `_shared/storageCopyVerification_test.ts`.
- [x] Migración de Storage histórica ejecutada (2026-09-18, autorizada por el propietario): 293 referencias actualizadas, 0 pendientes, 0 fallos; 17 huérfanos copiados y verificados; 1 resolución manual activa; originales conservados (632 objetos) y `delete_sources` sigue deshabilitado. Migraciones 0030–0035 aplicadas y operador raíz asignado.
- [ ] PENDIENTE (requiere decisión del propietario): la única referencia `company_settings.logo_url` no soportada (URL HTTPS ajena al Storage del proyecto) sigue sin clasificar. Desde 8.23.2 la app la trata fail-closed (no se renderiza ni se descarga; se usa el distintivo de respaldo). Sustituirla exige mutación de datos: subir de nuevo el logo desde Configuración → Logo, que ya persiste la RUTA de Storage. No ejecutado.
- [ ] Gates antes de dar de alta una segunda empresa: (1) branding por empresa resuelto y probado, (2) ensayo A/B aislado, (3) CI completo en verde, (4) recuperación verificada con restore ensayado (hoy sólo hay respaldo diario).
- [x] SNAPSHOT previo (ya ejecutado, ver línea anterior) — pendiente de ejecutar el migrador de Storage. Estado 2026-09-18 (reconciliado con el código real contra `public.organizations`): 322 objetos, **sólo 12 bajo prefijo exacto de organización**; un UUID ajeno no cuenta como prefijo. 305 filas de referencia = 293 candidatas + 11 ya prefijadas + 1 URL de logo no soportada; 18 objetos sin referencia = 17 sin prefijo + 1 con prefijo. De esos 17, **16 tienen dueño derivable** (15 por `supplier_bills.cfdi_uuid` en `supplier-bill-cfdi-xml` y 1 por `supplier_bills.id` en `supplier-payment-receipts`) y **1 sigue sin coincidencia**; ninguno tiene referencias. El `apply` normal **sigue bloqueado por los 17** objetos sin referencia y sin prefijo exacto. Los 15 legados **referenciados** (10 `cfdi-files`, 5 `documents`) son un tema distinto y sólo se mueven por el flujo protegido. Ambos ledgers en 0 filas: ninguna copia, referencia ni borrado. No se invocó apply/apply_orphans/delete_sources ni se habilitó bandera alguna. El **alta de una segunda empresa sigue bloqueada** hasta contener las fuentes legadas compartidas mediante el borrado separado, que continúa deshabilitado. [SNAPSHOT 2026-09-18, superado por la línea de arriba]
- [x] Migrador de Storage: atribución determinista de huérfanos (8.14.0). Se eliminó el atajo que, con una sola organización, asignaba a ella todos los objetos sin referencia y sin prefijo. Nuevo helper puro `_shared/storageOrphanOwner.ts`: resuelve el dueño sólo por coincidencia exacta del primer segmento con una única fila dueña conocida, según el call-site real de cada cubeta (`supplier-bill-cfdi-xml` ⇒ `supplier_bills.cfdi_uuid`; `supplier-payment-receipts` ⇒ `supplier_bills.id`); cero matches, varias filas o varias organizaciones ⇒ unresolved/conflict, sin asignación ni ledger. El modo plan reporta sólo conteos agregados por cubeta (sin referencia y sin prefijo, con dueño, sin dueño, en conflicto). `apply_orphans` sólo prepara los de dueño único, en copia + verificación, sin tocar referencias ni borrar; el borrado de huérfanos sigue prohibido y `apply` sigue fail-closed. Sin ampliación del ledger: el método se deriva de `bucket_id` y no se persiste evidencia del UUID fiscal. Pruebas en `_shared/storageOrphanOwner_test.ts`.
- [x] Migrador de Storage: coincidencia insensible a mayúsculas del UUID fiscal (8.14.2). `cfdi_uuid` es `text` con casing mixto en producción (67 mayúsculas / 2 minúsculas de 69 no nulos) y el filtro `.in(...)` en minúsculas falsos-negativeaba dueños reales. Ahora se consulta con `ilike` sin comodines sobre claves ya validadas como UUID, acotado a las claves de los huérfanos; `supplier_bills.id` conserva `.in(...)`. Indexación normalizada a minúsculas, sin perder detección de duplicados ni de varias organizaciones. Verificación de sólo lectura: 15 coincidencias únicas por UUID fiscal, 1 sin coincidencia, 1 comprobante único; sin operaciones de Storage.
- [x] Migrador de Storage: lectura acotada y fail-closed del índice de dueños (8.14.1). `loadOrphanOwnerIndex` ya no hace `SELECT` de toda `supplier_bills`: deriva primero las claves de los objetos sin referencia y sin prefijo, consulta sólo esas filas con filtros estructurados `.in(cfdi_uuid|id, …)` en bloques de 100, pagina hasta agotar (páginas de 500) y detecta truncación/error/duplicación anómala. Una clave cuya lectura no se agotó no se marca completa y resuelve `incomplete_lookup`: sin dueño, sin candidato y sin ledger. `delete_sources` ya no carga el índice y conserva intactas sus comprobaciones. Pruebas nuevas de truncación, completitud parcial por columna y derivación de claves.

- [x] Rollout 0024→0029 aplicado en producción el 2026-09-17 por el canal oficial de migraciones: las seis migraciones quedaron aplicadas como ids 25–30 con la huella exacta de los archivos actuales 0024–0029 (verificado por SELECT a `drizzle.__drizzle_migrations`). El baseline oficial confirmado es 0024–0029; `max(created_at)` aplicado = 1789750964000 (id 30 = 0029). Verificado por lecturas: índices únicos por empresa válidos, globales retirados, policies de profiles/user_roles por empresa, 19 policies de Storage con prefijo de empresa, 0 funciones objetivo con EXECUTE para anon. No se movieron objetos de Storage ni se modificaron datos. Backup disponible (2026-09-17 12:50:23 UTC); sin restore ensayado.
- [x] Anomalía histórica registrada, **sin identificar**: existe en el ledger un id 31 adicional con `created_at=1789683904941` (anterior al id 30) y `hash=927e6c306e8eacea96a1380dadfd6c3b0845af3cd46c00372cb9643dfe1ec98f`. Ese hash **no coincide** con ningún archivo actual de `drizzle/migrations` (el 0024 actual es `1a6bd55e…`). Su procedencia no está demostrada: **no se atribuye** a 0024 ni a 0030, no se borra ni se reescribe, y **no implica** que 0030 esté aplicada. Como su `created_at` es menor que el del id 30, no altera la selección del runner, que compara contra el máximo.
- [x] Nota histórica **obsoleta** (conservada como registro): la entrada previa decía que los artefactos 0030 habían sido eliminados del repo. En HEAD sí existen `drizzle/migrations/0030_multi_org_onboarding_and_customer_scope.sql`, su snapshot y la entrada idx 30 del journal, además de 0031–0035. 0030 está **presente en HEAD y pendiente en producción**.
- [x] Corrección 8.21.1 — CI de RLS en verde: `supabase/tests/rls/multi_org_scope_coverage.sql` exigía `org_scope_isolation` + `trg_organization_write_context` a **toda** tabla con `organization_id`, incluida `storage_migration_manual_resolutions` (0034), que es **infraestructura sensible** y no una tabla de negocio. Se añadió una **excepción exacta por nombre** (allowlist `c_infra_deny_all`, con verificación de que el nombre exista) sujeta a un contrato **más estricto**: RLS habilitada **y forzada**, policy RESTRICTIVE `FOR ALL TO authenticated, anon USING(false) WITH CHECK(false)`, **cero** policies PERMISSIVE para `anon`/`authenticated`/`public`, **cero** grants directos a `anon`/`authenticated`/`PUBLIC` y los 4 privilegios DML de `service_role`. Cualquier otra tabla con `organization_id` sigue **fail-closed** en la rama de negocio. Migración forward-only `0035_storage_manual_resolutions_force_rls.sql` (journal idx 35, `when` 1790269364000, posterior a 0034): `FORCE ROW LEVEL SECURITY` + revocación defensiva; sin datos, sin DDL destructiva. Validación en base PostgreSQL 17.9 efímera: 588/588 migraciones históricas, 36/36 Drizzle (0000–0035), **62/62 suites RLS**, **45/45 smoke**, `drizzle-kit check` OK y `src/test/drizzleJournalOrder.test.ts` 3/3. **0035 queda pendiente en producción**, igual que 0030–0034.
- [x] Corrección de orden temporal del journal (reconciliación 2026-09-18): las entradas idx 32–34 tenían `when` 1789700792739 / 1789701207301 / 1789701207302, **menores** que el baseline aplicado (1789750964000) y que idx 30–31. `drizzle-orm@0.45.2` (`pg-core/dialect.js`) sólo aplica migraciones cuyo `folderMillis` sea estrictamente mayor que el `created_at` más alto del ledger, así que 0032–0034 se habrían **omitido en silencio** en el próximo rollout. Se ajustó **únicamente** el `when` de esas tres migraciones pendientes (1790010164000 / 1790096564000 / 1790182964000); no se tocó el SQL, ni los hashes, ni migraciones aplicadas, ni el ledger de producción. Simulación con `readMigrationFiles` + baseline real: 5 pendientes (0030–0034), 30 cubiertas. Regresión nueva en `src/test/drizzleJournalOrder.test.ts` (orden estrictamente creciente, pendientes posteriores al baseline, correspondencia journal↔archivos); el job `rls-db-tests` ya valida además la cadena completa contra base efímera.
- [x] Migrador de Storage: parada de `apply` afinada (8.13.2). La condición dejó de mirar el total de objetos sin referencia y ahora sólo bloquea los **sin referencia y sin prefijo** de organización (`unreferenced_unscoped_objects`), que son legado sin dueño derivable. El resumen reporta por separado, global y por cubeta, `unreferenced_scoped_objects` y `unreferenced_unscoped_objects`. Los huérfanos ya prefijados (1 en producción al 2026-09-18; los otros 17 sin referencia no tienen prefijo y siguen bloqueando `apply`) se dejan intactos: sin ledger, sin recopia y sin borrado; `collectOrphanCandidates` ya los omitía. Un UUID cualquiera no cuenta como prefijo: sólo coincidencia exacta con una organización conocida. `apply_orphans` y `delete_sources` no se ejecutaron y el borrado de huérfanos sigue prohibido. Pruebas nuevas en `_shared/storageInventory_test.ts`.

## Multiempresa — cierre funcional (tramo 9, 8.15.0)

Implementado en el repositorio (migración `0030_multi_org_onboarding_and_customer_scope.sql`, journal idx 30) y validado localmente (Vitest 370 archivos / 2472 pruebas, typecheck, lint dirigido, `lint-migrations` de 0030, parse SQL, `arch:check`, build). Las suites RLS nuevas sólo corren en la base efímera de CI. **Sin producción**: 0030 no se ha aplicado a la base conectada.

- [x] 9.1 Alta de organizaciones: tabla `platform_operators` (RLS, respaldo inicial = administradores internos activos de la organización fundadora, sólo si la tabla está vacía) e `is_platform_operator()`; funciones sólo `service_role` con `assert_platform_operator(p_actor)`: `platform_create_organization`, `platform_attach_first_admin` (membresía interna + rol admin, atómico; rechaza un segundo primer administrador), `platform_discard_organization` (compensación que respeta la auditoría inmutable), `platform_set_organization_active`, `platform_list_organizations`. Server functions `src/lib/platformAdmin.functions.ts` (guard `requirePlatformOperator`, rate limit, alta de Auth con `organization_id` en metadatos, enlace de recuperación de un solo uso, compensación si falla Auth/attach) y pantalla "Empresas" (`/settings/organizations`, visible sólo cuando el servidor confirma al operador).
- [x] 9.2 Suspensión de empresa: `organization_scope_matches`/`resolve_organization_context` ignoran organizaciones inactivas (bloquea lectura/escritura org-scoped de miembros y portal); `trg_organization_active_flag` impide que un administrador de empresa cambie `is_active`; el contexto del navegador (`resolveOrganizationContext`) verifica `organizations.is_active` y muestra el motivo `organization_inactive`.
- [x] 9.3 Clientes por empresa: `customers.created_by_organization_id` fijado en el alta (`trg_customer_owner_organization`, sin adivinar empresa en procesos sin JWT con varias empresas), relación `organization_customers` creada para la empresa del usuario, policies de personal acotadas por `customer_scope_matches`, vínculo por RFC (`link_customer_to_organization_by_rfc`), archivado por relación cuando el cliente está compartido y global cuando la empresa es el único dueño. Frontend: `useCustomers` lista/detalla vía `organization_customers` + `customers!inner`.
- [x] 9.4 Portal por empresa: `inviteCustomerFn` decide por relación comercial activa con esta empresa y por `customer_portal_accounts (organization_id, customer_id)`; el vínculo legado `customers.user_id` sólo cuenta dentro de la empresa y sólo se escribe si estaba vacío; el indicador "acceso activo" usa `useCustomerPortalAccount` bajo RLS.
- [x] 9.5 Cobertura dinámica: `supabase/tests/rls/multi_org_scope_coverage.sql` recorre `information_schema`/`pg_policies`/`pg_trigger` y falla cerrado ante toda tabla pública de negocio con `organization_id` sin `org_scope_isolation` + trigger de contexto, o sin `organization_id` fuera de la allowlist explícita; RLS habilitada en todas las tablas públicas.
- [x] 9.6 Pruebas RLS `multi_org_onboarding.sql` (catálogo/ACL, alta de B por operador, primer admin atómico, `handle_new_user` con varias empresas, aislamiento de clientes A/B, vínculo por RFC, archivado por relación, portal por empresa, suspensión); Vitest de `requirePlatformOperator`, resolver de contexto, visibilidad de navegación y clientes; `docs/multiempresa/onboarding.md`; changelog 8.15.0.
- [x] APLICADO 2026-09-18 (8.22.0, autorización explícita del propietario): rollout de **0030, 0031, 0032, 0033, 0034 y 0035** a la base conectada por el canal oficial (`lov_database--migration`), en ese orden y con el contenido byte-por-byte de cada archivo. Preflight por SELECT: el ledger terminaba en ids 25–30 = 0024–0029 (más el id 31 extra no identificado) y ninguno de los seis hashes estaba presente. Resultado: ids 32–37 con hashes idénticos a `0030…0035` (`ee7dc399…`, `375518ba…`, `c394ad70…`, `96397084…`, `394f0ab0…`, `45f655ef…`). `src/integrations/supabase/types.ts` regenerado por el propio canal. Verificaciones posteriores (sólo lecturas): guard RESTRICTIVE `storage_objects_org_prefix_guard` presente, 20 policies de Storage con `storage_staff_path_in_current_organization`, `customers.created_by_organization_id` + índice, `storage_migration_manual_resolutions` con RLS **forzada** y sin privilegios para `anon`/`authenticated`, `organization_document_counters` con RLS y deny-all, y funciones `current_internal_organization_id`, `platform_grant_operator`, `platform_attach_first_admin`, `handle_new_user` instaladas. No se creó la segunda empresa, no se tocó Storage ni se desplegó nada.
- [ ] PENDIENTE (operativo, bloquea la pantalla "Empresas"): tras 0031 **no queda ningún `platform_operator`** en producción (lectura: 0 filas). Debe asignarse manualmente el primero por `service_role` antes de cualquier alta de empresa.
- [ ] PENDIENTE: alta real de la segunda empresa. **Sigue bloqueada** por los objetos legados de Storage: una fecha o un hash del ledger de migraciones **no demuestra** que un objeto de Storage esté aislado por prefijo de empresa ni autoriza el alta; esa autorización depende de la reconciliación de Storage (17 objetos sin referencia y sin prefijo, 15 legados referenciados, ledgers en 0 filas, borrado separado deshabilitado).

- [x] Tramo 10 (8.16.0) — Endurecimiento tras la auditoría multiempresa, **sólo repositorio**: migración forward-only `0031_multi_org_audit_hardening.sql` (journal idx 31) con Storage estricto por prefijo de empresa activa + policy RESTRICTIVE + bloqueo de empresa suspendida; `customer_scope_matches`/`soft_delete_customer` sólo con membresía interna verificada (sin fallback de una sola empresa, sin archivado global sin contexto); RLS deny-all y ACL sólo `service_role` en `organization_document_counters`; autoridad de plataforma explícita (retiro del seed automático + `platform_grant_operator`/`platform_revoke_operator`); alta de empresa en dos tiempos (inactiva hasta adjuntar su primer administrador en la misma transacción). Cuarentena agregada del migrador de Storage (`_shared/storageQuarantine.ts`) expuesta en el modo `plan`. Pruebas nuevas: `supabase/tests/rls/storage_strict_org_prefix_0031.sql`, `supabase/tests/rls/audit_hardening_0031.sql`, `_shared/storageQuarantine_test.ts`.
- [x] Tramo 10b (8.16.1) — Fixtures SQL alineados con 0030/0031 y verificación local ejecutada contra una instancia PostgreSQL efímera (588 migraciones Supabase + 32 Drizzle): contexto de organización y membresías internas explícitas en las suites heredadas; relación por organización, cuenta de portal activa y membresía `portal` en las suites del portal; suspensión vía `platform_set_organization_active` (operador de plataforma) en lugar de `UPDATE` directo; alta de empresa esperada como _pending_ hasta adjuntar al primer administrador; rechazo estricto de rutas sin prefijo en `r_fix32_portal_pagos_smoke.sql`; excepción explícita de `organization_document_counters` (deny-all deliberado) en `00_invariants.sql`. Resultado: **61/61 suites RLS** y **45/45 suites smoke** en verde. Sin producción, sin Storage real, sin deploy.
- [x] Tramo 11 (8.17.0) — Storage: las operaciones de **personal** exigen membresía interna, no sólo rol global. Migración forward-only `0032_multi_org_storage_staff_scope.sql` (journal idx 32) con `storage_staff_path_in_current_organization(text, boolean)` basada en `current_internal_organization_id()` (membresía `internal` única, empresa activa, sin membresía de portal) + prefijo exacto; reescritura de las 20 policies de personal en `documents`, `cfdi-files`, `supplier-bill-cfdi-xml`, `supplier-payment-receipts`, `feedback-screenshots` y la rama administrativa de `payment-proofs`; verificación fail-closed dentro de la propia migración. Las policies de objeto propio del portal (`payment_proof_path_allowed`, `customer_can_read_document_object`, capturas propias) y el guard RESTRICTIVE transversal no se relajaron. Regresión nueva en `supabase/tests/rls/storage_strict_org_prefix_0031.sql` (portal con rol `admin` residual: SELECT/INSERT/UPDATE/DELETE del personal denegados y sin efecto; conserva su comprobante propio). Resultado local: **61/61 suites RLS** en verde. Sin producción, sin Storage real, sin deploy.

- [x] APLICADO 2026-09-18 (8.22.0): **0030–0035** ejecutadas a la base conectada por el canal oficial, en orden y sin reescrituras (ledger ids 32–37, hashes idénticos a los archivos). `src/integrations/supabase/types.ts` regenerado por el canal. Queda **pendiente asignar manualmente el primer `platform_operator`** (tras 0031 no queda ninguno: 0 filas); hasta entonces la pantalla "Empresas" no es utilizable en producción.
- [ ] PENDIENTE (bloquea la segunda empresa): con 0031 los 15 objetos sin prefijo y los 295 con UUID ajeno dejan de ser legibles para el personal. Antes de habilitar una segunda empresa: ejecutar el **dry-run completo** del migrador (modo `plan`, sólo agregados) y **resolver o contener los históricos** (copy → verify → update references → observe → delete; el huérfano sin coincidencia requiere resolución manual vía 0034, con fuente intacta hasta aprobación separada).

- [x] Tramo 12 (8.18.0) — Alta de usuarios: el contexto de empresa deja de leerse de `raw_user_meta_data` (escribible por el usuario) y sólo se acepta desde `raw_app_meta_data` (service role). Migración forward-only `0033_handle_new_user_trusted_org_context.sql` (journal idx 33) con autoverificación por `pg_get_functiondef`; `auth.admin.createUser` de invitación interna, portal y primer administrador envía `app_metadata.organization_id`. Guards intactos: la empresa nace pendiente y se activa con `platform_attach_first_admin`. Prueba nueva `supabase/tests/rls/handle_new_user_trusted_org_context.sql` (falsificación hacia otra empresa sin cambio de contexto, sin audit_logs atribuidos y sin membresía) + control negativo. Evidencia: `signup_disabled` en el endpoint público de registro. Resultado local: **62/62 suites RLS**. Sin producción (0031–0034 pendientes de aplicar).

- [x] Tramo 12 (8.19.0) — Cierre operativo de la cuarentena de Storage. Migración forward-only `0034_storage_manual_resolutions.sql` (journal idx 34): tabla `public.storage_migration_manual_resolutions` con RLS activa, policy RESTRICTIVE deny-all para `anon`/`authenticated`, grants sólo a `service_role`, identidad única cubeta+ruta y justificación mínima. `evaluateManualResolution()` (puro, fail-closed) revalida antes de copiar: rechaza `no_manual_resolution`, `revoked`, `identity_mismatch`, `missing_justification`, `incomplete_lookup`, `unsupported_bucket`, `contradicts_derived_owner`, `unknown_organization`, `inactive_organization` y `revalidation_expired` (>24 h o futura). El migrador carga las resoluciones sólo en modos distintos de `delete_sources` y reporta `orphans.manual_resolution = { accepted, rejected }`: la respuesta sigue siendo **sólo agregada**, sin rutas, URLs, tokens ni ids, tampoco en logs. Procedimiento de inspección de sólo lectura (service_role, entorno privado, cruce `storage.objects` × relaciones dueñas) documentado en `docs/multiempresa/storage-historico.md`. 10/10 pruebas de cuarentena, `deno fmt --check`, `deno lint`, `deno check` y `drizzle-kit check` en verde; `lint-migrations` sin hallazgos en 0034. Sin producción: procedimiento no ejecutado, ninguna resolución registrada, ningún objeto movido.
- [x] Corrección 8.20.4 — Origen confiable de la empresa en el detector. Hallazgo: `ORG_SCOPE_PATTERNS` incluía `/\borganization_id\s*:/`, así que cualquier `insert`/`upsert` que mencionara la columna pasaba como seguro aunque el valor fuera `data.organization_id`; y un filtro con `organization_id` tampoco acreditaba que el valor viniera de la membresía. Corregido en `src/lib/__tests__/serverFnOrgIsolation.test.ts`: (a) la regla se separa por tipo de operación — `insert`/`upsert` deben ASIGNAR la empresa derivada en el payload, y `select`/`update`/`delete` deben FILTRAR por ella en la misma cadena; el patrón de payload ya no vale como alcance para una lectura; (b) se evalúa el ORIGEN del valor: se aceptan sólo identificadores derivados en servidor (`organizationId`, `orgId`, `ownOrganizationId`, `targetOrganizationId`, `callerOrganizationId`) y se rechaza cualquier expresión con raíz `data`/`input`/`payload`/`body`/`args`/`params`/`req`/`request`/`raw`/`meta(data)`; (c) `.match({...})` y `.or(...)` se evalúan igual, exigiendo que la interpolación sea confiable; (d) el veredicto distingue «sin alcance» de «empresa de origen no confiable», y **la allowlist sólo puede tapar el primero**: una empresa de origen no confiable falla siempre, aunque el archivo tenga excepción registrada; (e) el escáner cuenta escrituras inspeccionadas para que la rama insert/upsert no quede muerta. Casos negativos nuevos: filtro con `data.organization_id`, `insert` con `organization_id: input.data.organization_id`, `insert` sin columna de empresa y excepción intentando tapar origen no confiable; más el caso positivo de `insert` con la empresa derivada. **Límite declarado**: es análisis léxico, no de flujo de datos; comprueba el identificador usado, no que ese identificador provenga de la membresía. Tampoco cubre llamadas `rpc(...)`. Verificación manual de las rutas privilegiadas que escriben o filtran la empresa: `customerPortal.functions.ts` (membresía y cuenta de portal, ambas con `organizationId` derivado de `requireInternalOrganization`), `server/adminGuards.server.ts` (`createInternalMembership`, parámetro derivado por el llamador), `platformAdmin.functions.ts` (empresa creada por la RPC, `String(created.data)`), `userAdmin.functions.ts`, `feedbackAi.functions.ts`, `supplierRep.functions.ts` (`activity_feed` con el cliente del usuario, no privilegiado) y `pdf/contract/fetchers.ts` / `branding/resolveIssuerBranding.ts`. Único punto que acepta empresa del input: `setOrganizationActiveFn` y las RPC `platform_*`, tras `requirePlatformOperator`, con la autorización repetida dentro de la función SQL y con la prohibición de autosuspenderse. Resultado: 16/16 pruebas, 0 hallazgos. Sin producción.
- [x] Corrección 8.20.3 — Refuerzo de la regresión de aislamiento. La versión inicial de `src/lib/__tests__/serverFnOrgIsolation.test.ts` no acreditaba lo afirmado: sólo leía `src/lib/*.functions.ts`, usaba una lista de 15 tablas escrita a mano y daba por segura cualquier ruta que mencionara `requireInternalOrganization()` en cualquier parte del archivo, sin mirar la consulta. Ahora: (a) el conjunto de tablas con empresa se deriva de `src/integrations/supabase/types.ts` (toda tabla/vista cuyo `Row` tiene `organization_id`); (b) por cada `.from("tabla")` cuyo receptor es el cliente privilegiado (`admin`/`supabaseAdmin`/`adminClient`) se extrae la cadena completa de la operación y se exige alcance EN ESA operación (`.eq`/`.in("organization_id")`, `.match({organization_id})`, `.or(... organization_id ...)` o `organization_id:` en el payload); (c) se recorren `src/lib`, `src/lib/server`, `src/routes/api` (si existe) y `src/integrations/supabase`, no sólo `*.functions.ts`; (d) allowlist explícita por archivo + tabla + patrón exacto de la operación, con justificación, y prueba que falla si una excepción queda obsoleta; (e) prueba del propio detector (caso positivo y negativo) y verificación de que inspeccionó consultas reales. Se añadió además la comprobación en Vitest de los cinco endpoints retirados (un único `Deno.serve(makeRetiredEndpointHandler(...))`, sin código privilegiado residual ni imports sobrantes), en paralelo a la prueba Deno equivalente. Excepciones registradas y justificadas: compensaciones por identidad tras borrar la cuenta de autenticación (`organization_memberships`/`customer_portal_accounts` por `auth_user_id`, precedidas de `assertTargetInOrganization` donde aplica) y la lectura del vínculo legado que precisamente DERIVA la empresa. **Cobertura real declarada**: código TypeScript de servidor; las Edge Functions en Deno se cubren con sus propias pruebas Deno, no con este escáner. Resultado: 11/11 pruebas y `tsgo --noEmit` en verde. Sin producción.
- [x] Corrección 8.20.1 (auditoría final, P1) — `classifyFeedbackReportFn` (`src/lib/feedbackAi.functions.ts`) leía y actualizaba `feedback_reports` con el cliente `service_role` (`admin`), que **no aplica RLS**, autorizando sólo por `requireRole(['admin','administrativo'])` y filtrando por `id`. Un admin de la empresa A que conociera el `report_id` de la empresa B podía leer título, descripción, `context_json` y capturas, o sobrescribir `severity`/`module`/clasificación IA. Corrección: la empresa se deriva con `requireInternalOrganization(context.supabase, context.userId)` y tanto el `select` como el `update` añaden `.eq("organization_id", organizationId)`; un reporte ajeno responde 404 sin filtrar su existencia. Regresión nueva `src/lib/__tests__/serverFnOrgIsolation.test.ts` (reforzada en 8.20.3), más aserción específica de los dos filtros en la ruta de clasificación. Sin migración: el defecto era de código, `feedback_reports.organization_id` y su policy `org_scope_isolation` ya existían. Validado: 2/2 pruebas nuevas y `tsgo --noEmit` en verde. Sin producción, sin Storage, sin deploy.
- [x] Auditoría final multiempresa (8.20.1) — Revisado sin hallazgos adicionales: rutas server/`api`/edge con `service_role` (fuera del P1 anterior), consultas de `src/` sin fallback de una sola empresa (`company_settings` pide 2 filas y trata la duplicidad como error explícito; `resolveIssuerBranding` verifica que la fila devuelta siga siendo de la misma empresa), migraciones `0031–0034` y el ledger de cuarentena (paginación incompleta ⇒ `incomplete_lookup`, duplicados y organizaciones múltiples ⇒ conflicto, revocación respetada, `copied` terminal), bootstrap del primer `platform_operator` (alta explícita, sin auto-escalada; el respaldo automático quedó retirado en `0031`) y activación de empresas (`pending → active` sólo con primer admin adjunto).
- [x] Tramo 13 (8.20.0) — Auditoría final multiempresa: retiro fail-closed de las Edge Functions de administración que autorizaban por **rol global** (`requireRole`/`requireAdmin` sobre `user_roles`) y operaban con `service_role` sin resolver la organización del actor ni la del usuario objetivo: `invite-user`, `invite-customer`, `delete-user`, `reset-user-password` y `toggle-user-status`. En un ERP multiempresa eso permitiría que un admin de A invitara, borrara, desactivara o restableciera la contraseña de un usuario de B. Nuevo `_shared/retiredEndpoint.ts`: sólo responde CORS al preflight y `410 ENDPOINT_RETIRED_MULTITENANT` en cualquier otro método; nunca lee el cuerpo ni crea clientes privilegiados, ni con JWT de admin o `service_role`. Los reemplazos org-scoped ya en uso por la app son `src/lib/userAdmin.functions.ts` (`inviteUserFn`, `deleteUserFn`, `resetUserPasswordFn`, `toggleUserStatusFn`) y `src/lib/customerPortal.functions.ts#inviteCustomerFn`, que derivan la empresa de `requireInternalOrganization` y crean usuarios con `app_metadata.organization_id` (0033). Pruebas nuevas `_shared/retiredEndpoint_test.ts` (4 casos, incluida la no lectura del cuerpo) y los cinco `index_test.ts` ahora esperan 410. Verificado además que la regresión runtime del escenario 0032 (portal con rol interno residual) ya existe en `supabase/tests/rls/storage_strict_org_prefix_0031.sql` §6 y que ninguna consulta de `src/` depende de un fallback de una sola empresa. Resultado: **444 pruebas Deno offline**, `deno fmt --check`, `deno lint`, `deno check` y `tsgo --noEmit` en verde. Sin producción, sin Storage real, sin deploy.
- [x] Corrección documental 8.20.0 — `docs/multiempresa/tramo-8-1-rollout-folio-rep.md` seguía declarando el journal productivo en `0023` y `0025`/`0026` pendientes, contradiciendo el rollout aplicado el 2026-09-17. Se marcó como registro **histórico** con nota fechada: el estado vigente del rollout se lleva en `roadmap.md`; 0031–0034 siguen pendientes.
- [x] Corrección 8.19.4 — Alineación documental del checklist de rollout: `roadmap.md` y `docs/multiempresa/onboarding.md` seguían pidiendo aplicar sólo 0031/0032 (o 0031–0033) cuando las migraciones preparadas son **0031–0034**. Ahora ambos dejan explícito que 0031–0034 están pendientes (no ejecutadas) por el canal oficial, con los pasos posteriores: regenerar `types.ts`, bootstrap manual del primer `platform_operator`, dry-run completo del migrador (modo `plan`, sólo agregados) y resolución/contención de los históricos antes de habilitar la segunda empresa. Todo permanece unchecked; sin producción.
- [x] Corrección 8.19.3 — `copied` es estado terminal del lote de copia de huérfanos: la consulta aún incluía `status='copied'`, de modo que con más aprobados que `batchSize` esas filas (primeras en el orden `created_at`) volvían a llenar el lote en cada corrida —`ensureCopied()` sólo revalida destino y devuelve `copied`— y las `planned` posteriores nunca se alcanzaban. El conjunto pendiente ahora es `planned`+`failed` (`failed` se conserva para retry); `copied` queda fuera y no cuenta como `skipped`. Revalidar copias, si se requiere, será una fase explícita aparte que no compita por el lote. Regresión nueva: `batchSize=1` con ledger `[copied aprobado antiguo, planned aprobado nuevo]` ⇒ la corrida procesa el nuevo. Documentada la limitación de que revocar no borra destinos ya copiados, con el procedimiento privado de contención (fuente intacta, sin actualizar referencias, borrado sólo bajo aprobación separada y su propia bandera). 17/17 pruebas de cuarentena, `deno fmt --check`, `deno lint` y `deno check` en verde. Sin producción.
- [x] Corrección 8.19.2 — Sin starvation en el lote de huérfanos: `applyOrphanBatch()` aplicaba `.limit(batchSize)` al ledger ANTES de filtrar por la allowlist, así que filas antiguas no aprobadas al inicio del orden ahogaban para siempre a una fila aprobada posterior. Ahora el lote pagina el ledger (páginas de 500) y acumula filas aprobadas hasta reunir `batchSize` o agotar el conjunto, con tope total de exploración de 20,000 filas (fail-closed si se alcanza). Las no aprobadas se omiten sin cambios y cuentan como `skipped`; con allowlist vacía no se lee el ledger y `skipped=0` (semántica consistente: nada se exploró). Una aprobada que exceda el lote queda para la próxima corrida. Regresión nueva: más de `batchSize` filas no autorizadas antes de la autorizada ⇒ ninguna cambia y la autorizada sí avanza. 16/16 pruebas de cuarentena, `deno fmt --check`, `deno lint` y `deno check` en verde. Sin producción.
- [x] Corrección 8.19.1 — Fail-closed del lote de huérfanos: `applyOrphanBatch()` releía del ledger TODAS las filas `orphaned` en `planned/copied/failed` sin filtrarlas, de modo que una resolución manual aprobada en una corrida previa seguía autorizando la copia tras ser revocada, caducar o dejar de pasar la revalidación. Ahora cada corrida deriva su allowlist exacta `bucket + source_path + organization_id` (`makeApprovedOrphanKeySet`) y filtra el lote (`filterOrphanLedgerToApproved`): allowlist vacía ⇒ ninguna fila se procesa; empresa que no coincide ⇒ se ignora sin copiar ni cambiar estado; se reporta `skipped`. `delete_sources` sigue sin cargar índice de dueños ni resoluciones manuales. Regresiones nuevas en `storageQuarantine_test.ts` (vigente sí procesa; revocada/caduca/contradictoria no copia). 15/15 pruebas de cuarentena, 438 pruebas Deno, `deno fmt --check`, `deno lint` y `deno check` en verde. Sin producción.
- [x] Entrega 8.18.1 — Reparación de entrega: se restauraron exactamente las 188 migraciones históricas de `supabase/migrations/` que el merge `927d05c` había convertido en bloques `DO ... IF to_regprocedure(...) IS NOT NULL` (reescribía historia ya aplicada y podía omitir GRANT/REVOKE en silencio en una instalación limpia). Las correcciones quedan sólo forward-only en 0031–0033 y en fixtures/tests. Los sustitutos por extensiones ausentes (`pg_cron`) y por desórdenes históricos preexistentes se aplican **únicamente a la copia efímera en `/tmp`**, nunca al repositorio. `deno fmt` aplicado a `_shared/storageQuarantine_test.ts` y `migrate-storage-org-prefix/index.ts`; `deno fmt --check && deno lint` en verde. Smoke `r_fix32_portal_pagos_smoke.sql` alineado con el predicado estricto de staff de 0032. Reset completo en orden real: `supabase_ok=588`, `drizzle_ok=34`, **62/62 RLS**, **45/45 smoke SQL**, `lint-migrations` de 0031–0033 sin hallazgos, typecheck, build y **426 pruebas Deno** en verde. Sin producción.

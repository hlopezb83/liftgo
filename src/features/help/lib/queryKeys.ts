/**
 * Query key factories para la feature `help`.
 */
import { createEntityKeys } from "@/lib/query/createEntityKeys";

export const userManualKeys = createEntityKeys("user-manual");
export const userManualVersionKeys = createEntityKeys("user-manual-versions");

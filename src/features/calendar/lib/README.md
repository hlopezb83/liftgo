# calendar — capa `lib/`

Aún vacía. Convención `features/<x>/{components,hooks,lib,pages}`:
coloca aquí utilidades puras del dominio calendario (formateo de rangos,
generación de segmentos Gantt, colores de estado) cuando surjan, en vez
de meterlas en `components/` o `hooks/`.

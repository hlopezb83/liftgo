/**
 * Query key factories para la feature `damage`.
 */
import { createEntityKeys } from "@/lib/query/createEntityKeys";

export const damagePhotoCountsKeys = createEntityKeys("damage_photo_counts");

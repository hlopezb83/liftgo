# operations — capa `hooks/`

Aún vacía. Convención `features/<x>/{components,hooks,lib,pages}`:
cuando `operations` (configuración operativa: tarifas prellenadas,
catálogos de modelos, etc.) necesite hooks de datos (`useQuery` /
`useMutation` / `useEntityMutation`), colócalos aquí. **No** los pongas
inline en los componentes de `components/`.

version: 2
updates:
  # Frontend: ecosistema "bun".
  #
  # v7.297.2 — antes era "npm". Dependabot npm actualiza package.json pero NO
  # regenera `bun.lock`, así que `bun install --frozen-lockfile` (en
  # .github/actions/setup-bun-project) reventaba en TODOS los PRs de deps.
  # El ecosistema "bun" sí entiende el lockfile de texto `bun.lock`
  # (lockfileVersion 1) y lo regenera en el mismo commit del PR.
  #
  # Cadencia MENSUAL y agrupada: semanal con 5 PRs abiertos generaba más
  # revisiones y más corridas de CI que valor. Las vulnerabilidades reales no
  # esperan a la cadencia — Dependabot abre security updates aparte.
  - package-ecosystem: "bun"
    directory: "/"
    schedule:
      interval: "monthly"
      day: "monday"
      time: "06:00"
      timezone: "America/Monterrey"
    open-pull-requests-limit: 2
    labels:
      - "dependencies"
    commit-message:
      prefix: "chore"
      include: "scope"
    groups:
      # Un solo PR para desarrollo y otro para producción: menos ruido y menos
      # corridas de CI que un PR por paquete.
      dev-dependencies:
        dependency-type: "development"
        update-types: ["minor", "patch"]
      prod-dependencies:
        dependency-type: "production"
        update-types: ["minor", "patch"]
    # v7.302.4 — ignorar TypeScript 7.x hasta que typescript-eslint lo soporte.
    ignore:
      - dependency-name: "typescript"
        versions: [">= 7"]

  # GitHub Actions: un único PR agrupado al mes.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
      day: "monday"
      time: "06:00"
      timezone: "America/Monterrey"
    open-pull-requests-limit: 1
    labels:
      - "ci"
      - "dependencies"
    commit-message:
      prefix: "chore"
      include: "scope"
    groups:
      actions:
        update-types: ["minor", "patch"]

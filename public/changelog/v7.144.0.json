{
  "description": "Sentry: breadcrumbs de navegación + request context (tenant, usuario, rol, flow) en cada error",
  "changes": [
    "Sentry: breadcrumbs de navegación + request context (tenant, usuario, rol, flow) en cada error"
  ]
}
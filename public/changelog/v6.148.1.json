{
  "description": "Se aplica `deno fmt` a supabase/functions (12 archivos) y se ajusta knip.json para ignorar 8 exports de librerías internas reutilizables (field wrappers CustomerField/DateRangeField/ForkliftField, SupplierSelector, supplierKeys, SUPPLIER_BILLS_QK alias deprecado, translateByCode, LostReason). Restablece los jobs `Edge Functions (Deno smoke tests)` y `Knip` en verde.",
  "changes": [
    "Aplicado `deno fmt` a supabase/functions/** (download-cfdi, stamp-cfdi, cancel-cfdi, stamp-credit-note, stamp-payment-complement, classify-feedback-report, validate-receptor-tax-info, refresh-cancellation-status, generate-recurring-maintenance, parse-csf, generate-manual, _shared/supabaseClients).",
    "knip.json: ampliada la lista `ignore` con field wrappers reutilizables (CustomerField, DateRangeField, ForkliftField), SupplierSelector, supplierKeys, useSupplierBills, errorCatalog y crm/constants para respetar exports que forman parte de librerías internas."
  ]
}
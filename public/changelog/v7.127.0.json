{
  "description": "Sprint 3 · Ola 3.5 — UX-M1: QuoteForm migrado a RHF + Zod",
  "changes": [
    "QuoteForm.tsx migrado a `<Form>` + `<FormField>` + `Controller` (12 campos)",
    "Nuevo `useQuoteForm.ts` (useForm + zodResolver, defaults con `validUntil` a 30 días)",
    "Reescritura de `useQuotePrefill.ts`: `form.reset(buildPrefillValues, { keepDirty: false })` con guard por id",
    "`useQuoteFormLogic.ts`: `useWatch` granular, `useMemo` para totales, `form.reset(values)` post-mutación",
    "Fix schema: `rentalLineSchema` acepta cualquier tarifa > 0 (diaria/semanal/mensual) — antes exigía mensual",
    "Se elimina refine redundante de `nonEmptyId` y se normaliza `dateRangeSchema.partial()`",
    "Se activa `useUnsavedChangesGuard(isDirty && !isPending)` con guard nativo + confirmación",
    "Retiro de `useQuoteFormState.ts`, `quoteFormValidation.ts`, `quoteFormPrefillHelpers.ts`",
    "Tests: schema +5 (dailyRate/weeklyRate válidos, todas-en-0 rechazo, taxRate inválido, logisticsCost sin flag)",
    "Tests: `useQuotePrefill.test.tsx` (4): mapping de existingQuote, detección de logística, `isDirty=false` post-prefill, no re-hidrata"
  ]
}
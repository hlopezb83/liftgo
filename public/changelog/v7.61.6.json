{
  "description": "El CI estaba en rojo por un error de ESLint (no-useless-assignment) introducido en la migración a @use-gesture/react. Se reemplazó la asignación inútil `canceled = true` por la llamada real `cancel()` expuesta por useDrag, que sí cancela el gesto en la librería. Además se corrigió el orden de imports (import-x/order) en el mismo archivo. 1 error + 1 warning eliminados; 98 warnings restantes son deuda incremental no bloqueante.",
  "changes": [
    "Reemplazado `canceled = true` (reasignación inútil de una variable local destructurada) por la llamada real `cancel()` expuesta por `useDrag` de @use-gesture/react, que sí cancela el gesto en la librería.",
    "Corregido orden de imports (`import-x/order`) en `src/hooks/usePullToRefresh.ts`: `@use-gesture/react` antes que `react`.",
    "Resultado: ESLint pasa de `✖ 100 problems (1 error, 99 warnings)` a `98 warnings`, CI vuelve a verde. Los 98 warnings restantes son deuda incremental no bloqueante (import-x/order, react-compiler hints)."
  ]
}
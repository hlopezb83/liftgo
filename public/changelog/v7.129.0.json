{
  "description": "Sprint 3 · Ola 3.7 — UX-M2: SupplierBillForm blindado (guard + tests + fix Date)",
  "changes": [
    "Fix latente `useSupplierBillForm.ts`: comparación de fechas de cobertura ahora usa `getTime()` (antes `String()` lexicográfico)",
    "`useSupplierBillForm.ts`: integra `useUnsavedChangesGuard(open && isDirty && !isPending)`",
    "Nuevo `supplierBillFormSchema.test.ts` (10 tests) — validación de campos, monedas y cobertura",
    "Nuevo `useSupplierBillForm.test.tsx` (7 tests) — submit, prefill, due_date sugerido, guard",
    "Total: +17 tests (1104 → 1121)"
  ]
}
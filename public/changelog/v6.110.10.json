{
  "description": "Antes de armar el payload de la NC se consulta la factura fuente en Facturapi (client.invoices.retrieve) y se reutiliza el customer.legal_name exacto ya aceptado por el SAT, evitando CFDI40145 cuando la razón social local difiere de la CSF.",
  "changes": [
    "supabase/functions/stamp-credit-note/handler.ts: antes de armar el payload se hace client.invoices.retrieve(facturapi_invoice_id) y se reutiliza el customer.legal_name exacto que ya fue aceptado por el SAT.",
    "Si la consulta a Facturapi falla, se cae al sanitizeLegalName local como fallback.",
    "Se eliminó la pre-validación /tools/tax_id_validation en favor de esta estrategia más determinista."
  ]
}
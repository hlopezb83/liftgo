{
  "description": "Nuevo hook `useNavigateTransition` (`src/hooks/useNavigateTransition.ts`) que envuelve `useNavigate` en `startTransition` de React 19: la ruta actual permanece interactiva mientras el destino monta Suspense, chunks lazy o queries pesadas. Migradas las superficies de mayor tráfico: `GlobalSearch` (Ctrl+K), `AlertsRow` del dashboard (facturas vencidas, rentas vencidas, servicios pendientes), `InsuranceAlert`, `ExpiringContractsAlert`, y los headers compartidos `DetailPageHeader` y `FormPageHeader` — el botón 'Volver' de todas las páginas de detalle/formulario hereda la transición sin cambios en llamadas existentes.",
  "changes": [
    "Nuevo hook `useNavigateTransition` (`src/hooks/useNavigateTransition.ts`) que envuelve `useNavigate` en `startTransition` de React 19: la ruta actual permanece interactiva mientras el destino monta Suspense, chunks lazy o queries pesadas. Migradas las superficies de mayor tráfico: `GlobalSearch` (Ctrl+K), `AlertsRow` del dashboard (facturas vencidas, rentas vencidas, servicios pendientes), `InsuranceAlert`, `ExpiringContractsAlert`, y los headers compartidos `DetailPageHeader` y `FormPageHeader` — el botón 'Volver' de todas las páginas de detalle/formulario hereda la transición sin cambios en llamadas existentes."
  ]
}